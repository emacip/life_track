<%= form_for(@ecg) do |f| %>
  <% if @ecg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ecg.errors.count, "error") %> prohibited this ecg from being saved:</h2>

      <ul>
      <% @ecg.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
